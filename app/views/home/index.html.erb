<% if user_signed_in? %>
  <%= "#{current_user.nickname}さんは、ログインしています" %>
  <%= link_to "ログアウト" ,destroy_user_session_path, data: { turbo_method: :delete } %>
<% else %>
  <%= link_to "新規登録", new_user_registration_path %>
  <%= link_to "ログイン", new_user_session_path %>
<% end %>

<h1>moku</h1>

<!--session画面への遷移ボタン-->
<%= button_to "記録する", sections_path, method: :get, data: { turbo: false } %>
<%= button_to "フィードバック", feedbacks_path, method: :get, class: "btn btn-info", data: { turbo: false } %>
